<x-layouts.mitra title="Permintaan Wawancara">
    <div class="mb-6"><p class="text-xs font-semibold uppercase tracking-wider text-blue-600">Talent Pool</p><h1 class="mt-1 text-2xl font-bold text-slate-900">Permintaan Wawancara Saya</h1><p class="mt-1 text-sm text-slate-500">Pantau status pendekatan kandidat alumni.</p></div>
    <div class="space-y-3">@forelse($requests as $item)<article class="bg-white border border-slate-200 rounded-xl p-5 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4"><div><h2 class="font-semibold text-slate-900">{{ $item->alumni->nama }}</h2><p class="text-xs text-slate-500 mt-1">{{ $item->alumni->jurusan }} · {{ $item->alumni->headline ?: 'Profil alumni' }}</p><p class="text-sm text-slate-600 mt-3">{{ $item->message ?: 'Tidak ada pesan.' }}</p>@if($item->status === 'accepted' && $item->alumni->cv_path)<div class="mt-3 flex gap-3"><a class="text-xs font-semibold text-blue-600" href="{{ route('mitra.talent-pool.document', [$item->alumni, 'cv']) }}">Unduh CV</a>@if($item->alumni->portfolio_path)<a class="text-xs font-semibold text-blue-600" href="{{ route('mitra.talent-pool.document', [$item->alumni, 'portfolio']) }}">Unduh Portofolio</a>@endif</div>@endif</div><span class="self-start rounded-full px-3 py-1 text-xs font-semibold {{ $item->status === 'accepted' ? 'bg-emerald-50 text-emerald-700' : ($item->status === 'rejected' ? 'bg-red-50 text-red-700' : 'bg-amber-50 text-amber-700') }}">{{ match($item->status) { 'accepted' => 'Diterima', 'rejected' => 'Ditolak', default => 'Menunggu' } }}</span></article>@empty<div class="rounded-xl border border-dashed border-slate-300 bg-white p-10 text-center text-sm text-slate-500">Belum ada permintaan wawancara.</div>@endforelse</div><div class="mt-6">{{ $requests->links() }}</div>
</x-layouts.mitra>
